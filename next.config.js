/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode:false
}

module.exports = {
    images: {
      domains: ['www.lemeilleuravis.com'],
    },
  };
  module.exports = nextConfig
