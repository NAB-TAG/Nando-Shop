export namespace MathConstants{

    // Devuelve la constante de euler
    export const euler = () => {
        return Math.E;
    }

    // Devuelve el numero pi
    export const pi = () => {
        return Math.PI;
    }
}