import { Suspense } from "react"


const Dashboard = () => {
    return(
        // <Suspense fallback={<Loading />}>
            <p>
                children

            </p>
        // </Suspense>
    )
}

export default Dashboard